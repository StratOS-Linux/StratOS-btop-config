post_install() {
  echo "==> Btop configuration has been installed to /etc/skel/.config/btop"
  echo "==> To use it, copy it to your home directory:"
  echo "    cp -r /etc/skel/.config/btop ~/.config/"
}
